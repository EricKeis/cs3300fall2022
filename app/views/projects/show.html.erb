<div class="container">
  <p style="color: green"><%= notice %></p>

  <div class="">
    <%= render @project %>

    <div class="d-grid gap-2 d-md-block">
      <%= link_to "Edit", edit_project_path(@project), :class => "btn btn-warning btn-sm me-1" %>
      <%= link_to "Delete", @project, method: :delete, :class => "btn btn-danger btn-sm me-1" %>
      <%= link_to "Back to projects", projects_path, :class => "btn btn-secondary btn-sm" %>
    </div>
  </div>
</div>
